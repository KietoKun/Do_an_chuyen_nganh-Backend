package com.pizzastore.enums;

public enum DiscountType {
    PERCENTAGE,   // Giảm theo phần trăm (VD: value = 10 thì giảm 10%)
    FIXED_AMOUNT  // Giảm số tiền cứng (VD: value = 50000 thì giảm 50k)
}